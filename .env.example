# Optional. When set, the dev/preview server exposes POST /api/mapgen which asks
# Claude for a biome plan used to seed map generation. Without it the sim falls
# back to the deterministic procedural generator (identical feature set, no LLM).
ANTHROPIC_API_KEY=

# Optional overrides.
MAPGEN_MODEL=claude-opus-5
MAPGEN_EFFORT=low
